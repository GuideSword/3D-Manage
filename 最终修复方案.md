# 最终修复方案

## 步骤1：先查看错误日志

在本地电脑执行：

```bash
bash /home/huangjianpei/3D-Manage/快速诊断.sh
```

或者直接执行：

```bash
ssh root@101.37.28.116 'pm2 logs 3d-manage-api --err --lines 30 --nostream' | tail -35
```

**查看错误信息，告诉我具体的错误内容。**

## 步骤2：根据错误类型选择修复方案

### 方案A：如果仍然是路由错误

执行 Express 5.x 兼容修复：

```bash
bash /home/huangjianpei/3D-Manage/修复文件路由（Express5兼容）.sh
```

### 方案B：如果错误在其他地方

先暂时禁用文件路由，测试其他功能：

```bash
ssh root@101.37.28.116 << 'ENDSSH'
cd /opt/3d-manage-backend
cp server.js server.js.bak
sed -i "s|app.use('/api/files', require('./routes/files'));|// app.use('/api/files', require('./routes/files')); // 临时禁用|g" server.js
pm2 restart 3d-manage-api
sleep 3
pm2 status
curl http://localhost:3001/health
ENDSSH
```

如果服务正常启动，说明问题确实在文件路由。

### 方案C：完全重新部署（如果以上都不行）

```bash
ssh root@101.37.28.116 << 'ENDSSH'
cd /opt/3d-manage-backend

# 1. 停止服务
pm2 stop all
pm2 delete all

# 2. 重新解压代码
cd /opt
rm -rf 3d-manage-backend
mkdir -p 3d-manage-backend
cd 3d-manage-backend
tar -xzf /tmp/backend-deploy.tar.gz

# 3. 检查目录结构
if [ -d "backend" ]; then
    mv backend/* .
    rmdir backend
fi

# 4. 创建.env
cat > .env << EOF
PORT=3001
FRONTEND_URL=*
JWT_SECRET=$(openssl rand -base64 32)
UPLOAD_DIR=./uploads
API_BASE_URL=http://101.37.28.116:3001
EOF

# 5. 创建上传目录
mkdir -p uploads/models uploads/orders/attachments uploads/stock uploads/previews
chmod -R 755 uploads

# 6. 修复文件路由（使用兼容版本）
cat > routes/files.js << 'ENDFILE'
const express = require('express');
const router = express.Router();
const path = require('path');
const { getFile, getFileInfo, UPLOAD_DIR } = require('../config/storage');

router.get('/', async (req, res) => {
  res.json({ message: '文件服务' });
});

router.get('/:path', async (req, res) => {
  try {
    let filePath = decodeURIComponent(req.params.path);
    if (req.query.fullPath) {
      filePath = decodeURIComponent(req.query.fullPath);
    }
    filePath = filePath.replace(/^\//, '');
    
    const resolvedPath = path.resolve(path.join(UPLOAD_DIR, filePath));
    const resolvedDir = path.resolve(UPLOAD_DIR);
    if (!resolvedPath.startsWith(resolvedDir)) {
      return res.status(403).json({ error: 'Access denied' });
    }
    
    const fileInfo = await getFileInfo(filePath);
    const fileBuffer = await getFile(filePath);
    const ext = path.extname(filePath).toLowerCase();
    const mimeTypes = {
      '.stl': 'application/sla', '.obj': 'model/obj', '.3mf': 'model/3mf',
      '.png': 'image/png', '.jpg': 'image/jpeg', '.jpeg': 'image/jpeg',
      '.pdf': 'application/pdf', '.csv': 'text/csv',
    };
    
    res.setHeader('Content-Type', mimeTypes[ext] || 'application/octet-stream');
    res.setHeader('Content-Length', fileInfo.size);
    res.setHeader('Content-Disposition', `inline; filename="${path.basename(filePath)}"`);
    
    const range = req.headers.range;
    if (range) {
      const parts = range.replace(/bytes=/, '').split('-');
      const start = parseInt(parts[0], 10);
      const end = parts[1] ? parseInt(parts[1], 10) : fileInfo.size - 1;
      res.status(206);
      res.setHeader('Content-Range', `bytes ${start}-${end}/${fileInfo.size}`);
      res.setHeader('Accept-Ranges', 'bytes');
      res.setHeader('Content-Length', (end - start) + 1);
      res.send(fileBuffer.slice(start, end + 1));
    } else {
      res.send(fileBuffer);
    }
  } catch (error) {
    console.error('文件下载失败:', error);
    if (error.code === 'ENOENT') {
      res.status(404).json({ error: '文件不存在' });
    } else {
      res.status(500).json({ error: '文件下载失败: ' + error.message });
    }
  }
});

module.exports = router;
ENDFILE

# 7. 安装依赖
npm install --production

# 8. 启动服务
pm2 start server.js --name "3d-manage-api"
pm2 save
sleep 3

# 9. 测试
pm2 status
curl http://localhost:3001/health
ENDSSH
```

## 推荐执行顺序

1. **先执行快速诊断**：查看具体错误
2. **根据错误信息选择方案**：
   - 如果是路由错误 → 执行方案A
   - 如果不确定 → 执行方案B（暂时禁用文件路由）
   - 如果都不行 → 执行方案C（完全重新部署）

## 请先执行

```bash
bash /home/huangjianpei/3D-Manage/快速诊断.sh
```

**把输出结果发给我，我会根据具体错误提供精确的修复方案。**


