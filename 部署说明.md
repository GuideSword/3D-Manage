# 部署到服务器 101.37.28.116

## 快速部署（3步完成）

### 第1步：上传文件到服务器

在本地电脑执行：

```bash
cd /home/huangjianpei/3D-Manage
scp backend-deploy.tar.gz root@101.37.28.116:/tmp/
# 密码: huangjianpei123@
```

### 第2步：上传部署脚本并执行

```bash
# 上传部署脚本
scp server-setup.sh root@101.37.28.116:/tmp/
# 密码: huangjianpei123@

# 连接到服务器
ssh root@101.37.28.116
# 密码: huangjianpei123@

# 执行部署脚本
chmod +x /tmp/server-setup.sh
/tmp/server-setup.sh
```

### 第3步：验证部署

```bash
# 测试API（在服务器上）
curl http://localhost:3001/health

# 或从本地电脑测试
curl http://101.37.28.116:3001/health
```

应该返回：`{"status":"OK","timestamp":"..."}`

---

## 详细步骤（如果快速部署失败）

### 步骤1：连接到服务器

```bash
ssh root@101.37.28.116
# 密码: huangjianpei123@
```

### 步骤2：安装Node.js（如果未安装）

```bash
# 更新系统
apt-get update && apt-get upgrade -y

# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# 验证安装
node -v  # 应该显示 v20.x.x
npm -v   # 应该显示 10.x.x
```

### 步骤3：上传代码（从本地电脑执行）

```bash
# 在本地电脑执行
cd /home/huangjianpei/3D-Manage
scp backend-deploy.tar.gz root@101.37.28.116:/tmp/
# 密码: huangjianpei123@
```

### 步骤4：解压代码（在服务器上执行）

```bash
# 回到服务器终端
mkdir -p /opt/3d-manage-backend
cd /opt/3d-manage-backend
tar -xzf /tmp/backend-deploy.tar.gz
rm /tmp/backend-deploy.tar.gz
```

### 步骤5：配置环境变量

```bash
cd /opt/3d-manage-backend

# 创建.env文件
cat > .env << 'EOF'
PORT=3001
FRONTEND_URL=*
JWT_SECRET=$(openssl rand -base64 32)
UPLOAD_DIR=./uploads
API_BASE_URL=http://101.37.28.116:3001
EOF

# 生成JWT密钥并更新
JWT_SECRET=$(openssl rand -base64 32)
sed -i "s|JWT_SECRET=.*|JWT_SECRET=$JWT_SECRET|" .env

# 创建上传目录
mkdir -p uploads/models uploads/orders/attachments uploads/stock uploads/previews
chmod -R 755 uploads
```

### 步骤6：安装依赖

```bash
cd /opt/3d-manage-backend
npm install --production
```

### 步骤7：安装PM2并启动服务

```bash
# 安装PM2
npm install -g pm2

# 启动服务
pm2 start server.js --name "3d-manage-api"

# 保存进程列表
pm2 save

# 设置开机自启
pm2 startup
# 执行上面命令输出的命令（如果有）
```

### 步骤8：配置防火墙

```bash
# Ubuntu/Debian
ufw allow 3001/tcp
ufw reload

# 或 CentOS/RHEL
firewall-cmd --permanent --add-port=3001/tcp
firewall-cmd --reload
```

### 步骤9：验证部署

```bash
# 查看服务状态
pm2 status

# 查看日志
pm2 logs 3d-manage-api

# 测试API
curl http://localhost:3001/health
```

---

## 部署完成后的验证

### 1. 测试API（从本地电脑）

```bash
curl http://101.37.28.116:3001/health
```

应该返回：
```json
{"status":"OK","timestamp":"2024-11-14T..."}
```

### 2. 测试API端点

```bash
# 测试订单列表
curl http://101.37.28.116:3001/api/orders

# 应该返回订单列表（可能是空数组）
```

### 3. 查看服务状态（SSH连接到服务器）

```bash
ssh root@101.37.28.116
pm2 status
```

应该显示 `3d-manage-api` 服务正在运行。

---

## 前端配置

前端已自动配置为服务器地址：
- API地址: `http://101.37.28.116:3001/api`

如果前端需要更新，编辑 `constants/index.js`：

```javascript
export const API_CONFIG = {
  BASE_URL: 'http://101.37.28.116:3001/api',
  TIMEOUT: 30000,
};
```

---

## 常用管理命令

### 查看服务状态

```bash
ssh root@101.37.28.116 'pm2 status'
```

### 查看日志

```bash
ssh root@101.37.28.116 'pm2 logs 3d-manage-api'
```

### 重启服务

```bash
ssh root@101.37.28.116 'pm2 restart 3d-manage-api'
```

### 停止服务

```bash
ssh root@101.37.28.116 'pm2 stop 3d-manage-api'
```

### 查看实时日志（最后100行）

```bash
ssh root@101.37.28.116 'pm2 logs 3d-manage-api --lines 100'
```

---

## 故障排查

### 服务无法启动

```bash
ssh root@101.37.28.116
cd /opt/3d-manage-backend
pm2 logs 3d-manage-api
```

查看错误信息，常见问题：
- 端口被占用
- Node.js版本不对
- 依赖安装失败
- 环境变量配置错误

### 端口被占用

```bash
ssh root@101.37.28.116
netstat -tulpn | grep 3001
```

如果端口被占用，可以：
- 修改 `.env` 中的 `PORT` 为其他端口（如3002）
- 或者停止占用端口的服务

### 防火墙阻止

```bash
ssh root@101.37.28.116
ufw status
```

确保3001端口已开放。

### 无法从外部访问

1. 检查防火墙：`ufw status`
2. 检查服务是否运行：`pm2 status`
3. 检查服务是否监听所有接口：`netstat -tulpn | grep 3001`

---

## 下一步

部署完成后：
1. ✅ 测试API是否正常
2. ✅ 启动前端应用
3. ✅ 测试前端是否能连接到后端
4. ✅ 测试订单管理功能

如有问题，查看日志：`pm2 logs 3d-manage-api`


