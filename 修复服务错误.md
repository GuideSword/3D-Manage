# 修复服务错误状态

## 问题

服务状态显示：
- status: `errored` (错误)
- ↺: 17 (已重启17次)
- memory: 0b (无内存占用，服务崩溃)

这说明服务启动失败，需要查看日志找出问题。

## 立即执行（在服务器上）

### 步骤1：查看错误日志

```bash
cd /opt/3d-manage-backend

# 查看最近的错误日志（最后100行）
pm2 logs 3d-manage-api --lines 100

# 或查看所有错误日志
pm2 logs 3d-manage-api --err
```

这会显示具体的错误信息，请把错误信息发给我。

### 步骤2：手动启动服务查看错误

```bash
cd /opt/3d-manage-backend

# 停止PM2服务（避免重复启动）
pm2 stop 3d-manage-api
pm2 delete 3d-manage-api

# 手动启动，查看实时错误
node server.js
```

按 Ctrl+C 停止，然后把错误信息发给我。

## 常见错误和解决方案

### 错误1：找不到模块

```
Error: Cannot find module 'express'
```

**解决**：
```bash
cd /opt/3d-manage-backend
npm install --production
```

### 错误2：端口被占用

```
Error: listen EADDRINUSE: address already in use :::3001
```

**解决**：
```bash
# 查找占用端口的进程
lsof -i :3001
# 或
netstat -tulpn | grep 3001

# 杀死进程
kill -9 <PID>

# 或修改.env中的PORT
nano .env
# 改为 PORT=3002
```

### 错误3：找不到配置文件

```
Error: Cannot find module './config/storage'
```

**解决**：
```bash
cd /opt/3d-manage-backend

# 检查文件是否存在
ls -la config/storage.js

# 如果不存在，需要重新解压文件
tar -xzf /tmp/backend-deploy.tar.gz

# 检查目录结构
if [ -d "backend" ]; then
    mv backend/* .
    rmdir backend
fi
```

### 错误4：环境变量错误

```
Error: Invalid configuration
```

**解决**：
```bash
cd /opt/3d-manage-backend

# 检查.env文件
cat .env

# 重新创建.env
cat > .env << 'EOF'
PORT=3001
FRONTEND_URL=*
JWT_SECRET=$(openssl rand -base64 32)
UPLOAD_DIR=./uploads
API_BASE_URL=http://101.37.28.116:3001
EOF

# 生成JWT密钥
JWT_SECRET=$(openssl rand -base64 32)
sed -i "s|JWT_SECRET=.*|JWT_SECRET=$JWT_SECRET|" .env
```

### 错误5：文件权限问题

```
Error: EACCES: permission denied
```

**解决**：
```bash
cd /opt/3d-manage-backend
chmod -R 755 .
chmod 644 server.js
chmod -R 755 uploads
```

### 错误6：Node.js版本不兼容

```
Error: SyntaxError: Unexpected token
```

**解决**：
```bash
# 检查Node.js版本
node -v

# 应该 >= v18，如果版本太低，升级Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# 重启服务
pm2 restart 3d-manage-api
```

## 快速修复脚本

如果以上都不行，执行完整修复：

```bash
cd /opt/3d-manage-backend

# 1. 停止并删除服务
pm2 stop 3d-manage-api
pm2 delete 3d-manage-api

# 2. 检查必要文件
echo "检查文件..."
ls -la package.json server.js config/storage.js

# 3. 检查并修复目录结构
if [ -d "backend" ]; then
    echo "修复目录结构..."
    mv backend/* .
    mv backend/.* . 2>/dev/null || true
    rmdir backend
fi

# 4. 重新安装依赖
echo "重新安装依赖..."
rm -rf node_modules
npm install --production

# 5. 创建.env（如果不存在）
if [ ! -f ".env" ]; then
    echo "创建.env文件..."
    JWT_SECRET=$(openssl rand -base64 32)
    cat > .env << EOF
PORT=3001
FRONTEND_URL=*
JWT_SECRET=$JWT_SECRET
UPLOAD_DIR=./uploads
API_BASE_URL=http://101.37.28.116:3001
EOF
fi

# 6. 创建上传目录
mkdir -p uploads/models uploads/orders/attachments uploads/stock uploads/previews
chmod -R 755 uploads

# 7. 手动测试启动（查看错误）
echo "手动测试启动..."
node server.js
# 如果这里出错，请把错误信息发给我
# 按 Ctrl+C 停止

# 8. 使用PM2启动
echo "使用PM2启动..."
pm2 start server.js --name "3d-manage-api"
pm2 save

# 9. 等待启动
sleep 3

# 10. 查看状态
pm2 status

# 11. 查看日志
pm2 logs 3d-manage-api --lines 50

# 12. 测试API
curl http://localhost:3001/health
```


