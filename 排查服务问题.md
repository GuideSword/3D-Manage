# 排查服务无法连接的问题

服务无法连接到 3001 端口，请按照以下步骤排查：

## 步骤1：检查服务是否运行

```bash
ssh root@101.37.28.116

# 检查PM2服务状态
pm2 status

# 查看服务列表
pm2 list
```

如果服务不存在或已停止，需要启动服务。

## 步骤2：查看服务日志

```bash
# 查看最近的日志（最后100行）
pm2 logs 3d-manage-api --lines 100

# 或查看所有日志
pm2 logs 3d-manage-api
```

查看是否有错误信息。

## 步骤3：检查端口是否被占用

```bash
# 检查3001端口
netstat -tulpn | grep 3001
# 或
lsof -i :3001
```

如果没有输出，说明服务没有监听3001端口。

## 步骤4：手动启动服务（如果服务未运行）

```bash
cd /opt/3d-manage-backend

# 检查package.json和server.js是否存在
ls -la package.json server.js

# 检查.env文件是否存在
ls -la .env

# 尝试手动启动（查看错误信息）
node server.js
```

按 Ctrl+C 停止，然后查看错误信息。

## 步骤5：使用PM2启动服务

```bash
cd /opt/3d-manage-backend

# 停止旧服务（如果存在）
pm2 stop 3d-manage-api 2>/dev/null || true
pm2 delete 3d-manage-api 2>/dev/null || true

# 启动服务
pm2 start server.js --name "3d-manage-api"

# 查看状态
pm2 status

# 查看日志
pm2 logs 3d-manage-api
```

## 步骤6：检查依赖是否安装

```bash
cd /opt/3d-manage-backend

# 检查node_modules是否存在
ls -la node_modules | head

# 如果node_modules不存在或为空，重新安装
npm install --production

# 验证关键依赖
npm list express multer dotenv
```

## 步骤7：检查环境变量

```bash
cd /opt/3d-manage-backend

# 查看.env文件内容
cat .env

# 验证格式是否正确
# 应该包含：
# PORT=3001
# FRONTEND_URL=*
# JWT_SECRET=...
# UPLOAD_DIR=./uploads
# API_BASE_URL=http://101.37.28.116:3001
```

## 步骤8：检查文件权限

```bash
cd /opt/3d-manage-backend

# 检查文件和目录权限
ls -la

# 确保server.js可读
chmod 644 server.js

# 确保uploads目录可写
chmod -R 755 uploads
```

## 常见错误和解决方案

### 错误1：找不到模块

```
Error: Cannot find module 'express'
```

**解决**：
```bash
cd /opt/3d-manage-backend
npm install --production
```

### 错误2：端口被占用

```
Error: listen EADDRINUSE: address already in use :::3001
```

**解决**：
```bash
# 查找占用端口的进程
lsof -i :3001
# 或
netstat -tulpn | grep 3001

# 杀死进程
kill -9 <PID>

# 或修改.env中的PORT为其他端口（如3002）
```

### 错误3：权限错误

```
Error: EACCES: permission denied
```

**解决**：
```bash
cd /opt/3d-manage-backend
chmod -R 755 .
```

### 错误4：找不到.env文件

**解决**：
```bash
cd /opt/3d-manage-backend
cat > .env << 'EOF'
PORT=3001
FRONTEND_URL=*
JWT_SECRET=$(openssl rand -base64 32)
UPLOAD_DIR=./uploads
API_BASE_URL=http://101.37.28.116:3001
EOF
```

## 完整重新部署流程

如果以上都不行，完整重新部署：

```bash
ssh root@101.37.28.116
cd /opt/3d-manage-backend

# 1. 停止所有服务
pm2 stop all
pm2 delete all

# 2. 清理
rm -rf node_modules
rm -rf uploads
rm -f .env

# 3. 重新解压（如果文件还在/tmp）
tar -xzf /tmp/backend-deploy.tar.gz

# 4. 创建.env
JWT_SECRET=$(openssl rand -base64 32)
cat > .env << EOF
PORT=3001
FRONTEND_URL=*
JWT_SECRET=$JWT_SECRET
UPLOAD_DIR=./uploads
API_BASE_URL=http://101.37.28.116:3001
EOF

# 5. 创建上传目录
mkdir -p uploads/models uploads/orders/attachments uploads/stock uploads/previews
chmod -R 755 uploads

# 6. 安装依赖
npm install --production

# 7. 启动服务
pm2 start server.js --name "3d-manage-api"
pm2 save

# 8. 查看状态和日志
pm2 status
pm2 logs 3d-manage-api --lines 50
```

## 验证部署

```bash
# 等待几秒后测试
sleep 5
curl http://localhost:3001/health

# 应该返回：{"status":"OK","timestamp":"..."}
```


