# 3D打印管理系统 - 服务器部署指南

本指南将帮助你将后端服务部署到自己的服务器上，并配置前端连接到服务器。

## 前置要求

- 一台Linux服务器（Ubuntu/CentOS推荐）
- Node.js v18+ 已安装
- PostgreSQL 数据库（可选，当前使用内存数据）
- 域名和SSL证书（可选，用于HTTPS）

## 一、服务器准备

### 1. 连接到服务器

```bash
ssh user@your-server-ip
```

### 2. 安装 Node.js（如果未安装）

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs

# 验证安装
node -v
npm -v
```

### 3. 安装 PostgreSQL（可选，如果使用数据库）

```bash
# Ubuntu
sudo apt update
sudo apt install postgresql postgresql-contrib

# 启动服务
sudo systemctl start postgresql
sudo systemctl enable postgresql

# 创建数据库
sudo -u postgres psql
CREATE DATABASE "3d_manage";
CREATE USER "3d_user" WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE "3d_manage" TO "3d_user";
\q
```

## 二、部署后端服务

### 1. 上传代码到服务器

```bash
# 方法1：使用Git
cd /opt
git clone your-repo-url
cd 3d-manage/backend

# 方法2：使用SCP上传
# 在本地执行
scp -r backend/ user@your-server-ip:/opt/3d-manage-backend/
```

### 2. 安装依赖

```bash
cd /opt/3d-manage/backend  # 或你的路径
npm install
```

### 3. 配置环境变量

```bash
cp .env.example .env
nano .env  # 或使用vim
```

编辑 `.env` 文件：

```env
PORT=3001
FRONTEND_URL=*
JWT_SECRET=your-super-secret-jwt-key-change-this
UPLOAD_DIR=./uploads
API_BASE_URL=https://api.yourdomain.com
```

> 注意：生产环境 `FRONTEND_URL` 可以设置为 `*` 或具体的前端域名

### 4. 创建上传目录

```bash
mkdir -p uploads/models uploads/orders/attachments uploads/stock uploads/previews
chmod -R 755 uploads
```

### 5. 启动服务（使用PM2）

```bash
# 安装PM2
sudo npm install -g pm2

# 启动服务
cd /opt/3d-manage/backend
pm2 start server.js --name "3d-manage-api"

# 保存进程列表
pm2 save

# 设置开机自启
pm2 startup
# 执行输出的命令

# 查看状态
pm2 status
pm2 logs 3d-manage-api
```

### 6. 配置防火墙

```bash
# Ubuntu (ufw)
sudo ufw allow 3001/tcp
sudo ufw reload

# CentOS (firewalld)
sudo firewall-cmd --permanent --add-port=3001/tcp
sudo firewall-cmd --reload
```

## 三、配置Nginx反向代理（推荐）

### 1. 安装Nginx

```bash
# Ubuntu
sudo apt install nginx

# CentOS
sudo yum install nginx
```

### 2. 配置Nginx

创建配置文件：

```bash
sudo nano /etc/nginx/sites-available/3d-manage-api
```

内容：

```nginx
server {
    listen 80;
    server_name api.yourdomain.com;  # 替换为你的域名

    # 文件上传大小限制
    client_max_body_size 500M;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置（大文件上传需要）
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # 静态文件缓存（可选）
    location /api/files/ {
        proxy_pass http://localhost:3001;
        proxy_cache_valid 200 1h;
        expires 1h;
        add_header Cache-Control "public, immutable";
    }
}
```

### 3. 启用配置

```bash
# Ubuntu
sudo ln -s /etc/nginx/sites-available/3d-manage-api /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# CentOS
sudo ln -s /etc/nginx/conf.d/3d-manage-api.conf /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 4. 配置SSL（使用Let's Encrypt）

```bash
# 安装certbot
sudo apt install certbot python3-certbot-nginx  # Ubuntu
# 或
sudo yum install certbot python3-certbot-nginx  # CentOS

# 申请证书
sudo certbot --nginx -d api.yourdomain.com

# 自动续期已配置
```

## 四、前端配置

### 1. 修改API地址

编辑 `constants/index.js`：

```javascript
export const API_CONFIG = {
  BASE_URL: 'https://api.yourdomain.com/api',  // 你的服务器地址
  TIMEOUT: 30000,
};
```

### 2. 测试连接

```bash
# 在浏览器中访问
https://api.yourdomain.com/health

# 应该返回
{"status":"OK","timestamp":"..."}
```

## 五、文件存储说明

### 存储目录结构

```
backend/uploads/
├── models/          # 模型文件（STL/OBJ/3MF）
├── orders/
│   └── attachments/ # 订单附件（图片/PDF）
├── stock/           # 库存批次图片
└── previews/        # 预览图
```

### 文件访问

- **上传**: `POST /api/models/upload` 或 `POST /api/orders/upload-attachment`
- **下载**: `GET /api/files/:filePath`
- **示例**: `https://api.yourdomain.com/api/files/models/12345-abc-model.stl`

### 备份建议

```bash
# 创建备份脚本
cat > /opt/backup-uploads.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/opt/backups/uploads"
DATE=$(date +%Y%m%d_%H%M%S)
tar -czf "$BACKUP_DIR/uploads_$DATE.tar.gz" /opt/3d-manage/backend/uploads
# 删除7天前的备份
find $BACKUP_DIR -name "*.tar.gz" -mtime +7 -delete
EOF

chmod +x /opt/backup-uploads.sh

# 添加到crontab（每天凌晨2点备份）
crontab -e
# 添加：
0 2 * * * /opt/backup-uploads.sh
```

## 六、监控和维护

### 1. 查看服务状态

```bash
pm2 status
pm2 logs 3d-manage-api --lines 100
```

### 2. 重启服务

```bash
pm2 restart 3d-manage-api
```

### 3. 查看磁盘使用

```bash
du -sh /opt/3d-manage/backend/uploads
df -h
```

### 4. 清理日志

```bash
pm2 flush  # 清空PM2日志
```

## 七、故障排查

### 服务无法启动

1. 检查端口是否被占用：`netstat -tulpn | grep 3001`
2. 查看错误日志：`pm2 logs 3d-manage-api`
3. 检查环境变量：`cat .env`

### 文件上传失败

1. 检查上传目录权限：`ls -la uploads/`
2. 检查磁盘空间：`df -h`
3. 检查Nginx配置中的 `client_max_body_size`

### 前端无法连接

1. 检查CORS配置：确认 `FRONTEND_URL` 正确
2. 检查防火墙：`sudo ufw status`
3. 测试API：`curl https://api.yourdomain.com/health`

## 八、安全建议

1. **修改JWT密钥**: 生产环境使用强密钥
2. **配置HTTPS**: 必须使用SSL证书
3. **限制访问**: 使用防火墙限制IP访问（如需要）
4. **定期备份**: 设置自动备份数据库和文件
5. **更新依赖**: 定期运行 `npm audit` 和 `npm update`

## 九、性能优化

1. **使用CDN**: 为文件下载配置CDN（可选）
2. **Nginx缓存**: 已配置静态文件缓存
3. **数据库索引**: 如果使用数据库，添加必要索引
4. **负载均衡**: 高并发时可配置多实例+负载均衡

---

部署完成后，你的API将可以通过 `https://api.yourdomain.com` 访问！


