# 深度排查服务错误

服务仍然是 errored 状态，需要深入排查。

## 步骤1：查看最新错误日志

```bash
ssh root@101.37.28.116 'pm2 logs 3d-manage-api --err --lines 50 --nostream'
```

## 步骤2：检查文件是否正确修复

```bash
ssh root@101.37.28.116 'cat /opt/3d-manage-backend/routes/files.js | head -15'
```

检查：
- 第7行应该是：`router.get('/*', async (req, res) => {`
- 第9行应该是：`const filePath = req.params[0] || ...`

## 步骤3：手动测试启动

```bash
ssh root@101.37.28.116 << 'ENDSSH'
cd /opt/3d-manage-backend
pm2 stop 3d-manage-api
pm2 delete 3d-manage-api
node server.js
ENDSSH
```

按 Ctrl+C 停止，查看错误信息。

## 步骤4：检查所有依赖文件

```bash
ssh root@101.37.28.116 << 'ENDSSH'
cd /opt/3d-manage-backend
echo "检查文件..."
ls -la server.js
ls -la routes/files.js
ls -la config/storage.js
echo ""
echo "检查语法..."
node -c server.js && echo "server.js 语法正确" || echo "server.js 语法错误"
node -c routes/files.js && echo "files.js 语法正确" || echo "files.js 语法错误"
node -c config/storage.js && echo "storage.js 语法正确" || echo "storage.js 语法错误"
ENDSSH
```


