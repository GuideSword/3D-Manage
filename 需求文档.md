# React Native 3D 打印管理系统 — **管理版 MVP** 需求 v0.2

> 目标：先上“管理能力”，把账目、清单、文件与状态管起来；复杂能力（排程/自动报价/集成/通知/报表/三维预览）延后。
> 平台：iOS / Android（React Native）｜后端：REST（Node/NestJS/Express 均可）+ SQL 数据库（PostgreSQL/MySQL）

---

## 0. 范围（本期只做）

* 三大板块仅提供 **基础 CRUD + 状态流转 + 导入/导出**：

  * 订单管理：手录/导入订单，简单状态流转，金额**人工录入**；不做自动报价、排程、发货跟踪。
  * 模型管理：上传与版本记录、元数据维护；三维预览延后（先缩略图/占位）。
  * 耗材管理：物料主数据、库存批次、手工入出库与盘点；不做预测、预警、采购流程。
* 权限：Owner / Staff（通用编辑）/ Viewer（只读）。
* 国际化：中文优先；英文延后。

> **暂不做**：打印机直连与排程、Shopify/Etsy 集成、OctoPrint/Klipper 接口、消息推送、复杂报表、AI 报价/分析。

## 1. 角色与权限（简版）

* **Owner**：全部权限；用户与系统配置；数据导出。
* **Staff**：订单/模型/耗材的新增、编辑、删除；查看审计。
* **Viewer**：只读。

> 权限粒度（本期）：模块级（读/写/删/导出）。对象级、字段级放入后续迭代。

## 2. 核心流程（简化）

* **订单流**：创建 → 待审核 → 执行中 → 已完成 / 已取消。
* **模型流**：上传 → 新版本/备注 → 绑定到订单条目（可选）。
* **耗材流**：建物料 → 建批次 → 手工入/出/盘点；可在备注中写入“关联订单号”。

## 3. 功能需求

### 3.1 订单管理（MVP 核心）

* **来源**：手工创建；CSV/Excel 批量导入；搜索/筛选（客户、状态、日期）。
* **字段**：客户信息、收货地址、订单行（可选绑定模型或外部文件链接）、材料/颜色/层高/数量、单价与总价（**人工录入**）、期望交期、备注、附件（图片/PDF）。
* **状态机**：`草稿 → 待审核 → 执行中 → 已完成/已取消`；记录原因与审计日志。
* **操作**：增/改/删、状态变更、附件上传、CSV 导出。

### 3.2 模型管理（MVP 必备）

* **上传格式**：STL/OBJ/3MF（仅存储，不解析体积）；显示文件名/大小/SHA256/上传者/时间。
* **版本管理**：同名模型可新增版本；展示版本时间线与备注。
* **可见性**：私有/团队；对外分享延后。
* **预览**：本期仅缩略图/占位图；三维 WebView/GL 预览放入后续。

#### 3.2.A 上传与存储方案（RN App 专项）

**存储位置**：集中放在**对象存储**（S3 兼容，如 AWS S3 / MinIO / 阿里云 OSS / GCS / Azure Blob）。**App 本地仅作临时缓存**（沙盒缓存目录），上传完成即可清理。

**对象键命名**（示例）：

```
models/{tenantId}/{assetId}/{version}/{originalFileName}
预览图：previews/{assetId}/{version}.png
```

> 优点：后端与前端解耦、可挂 CDN、支持大文件分片与断点续传。

**上传流程（直传推荐）**：

1. App 调用 `POST /upload/init`，携带文件名、大小、MIME、assetId（可空）。
2. 后端校验后返回：`uploadId`、`objectKey`、**预签名 URL 列表**（S3 Multipart/OSS 分片）或单个表单参数（小文件）。
3. App 直接把分片上传到对象存储（带进度回调/可并发）。
4. 传完调用 `POST /upload/complete`（含各分片 ETag），后端向存储发起合并；生成 `file_key` 与 `sha256`。
5. App 再 `POST /models/:id/versions` 写入版本记录（指向 `file_key`）。

**后端代理上传（可选）**：若需**合规模块/病毒扫描**先行，可让 App 把文件先传到后端临时桶，由后端转存对象存储。成本更高但控制更强。

**安全与合规**：

* 预签名 URL 设置短时效（如 10 分钟），最小权限（仅 `putObject`）。
* 限制扩展名/MIME/大小（如 ≤ 500MB），落库保存 `sha256` 与上传者。
* 服务端完成后触发**异步任务**（可选）：病毒扫描、重命名、缩略图生成、网格轻量校验（非流形/翻面）。

**离线与续传**：

* 分片大小建议 5–15MB；失败自动重试；记录已完成分片清单。
* RN 本地采用沙盒缓存路径，上传完成后可删除。

**RN 实现提示**：

* Expo：`expo-file-system` 做本地访问；通过 `fetch`/`XMLHttpRequest` 上传到预签名 URL；大文件可选原生背景上传库（bare 模式）。
* bare RN：`react-native-fs`/`react-native-blob-util` 读取分片；实现 S3 Multipart/OSS 分片接口；或接入支持 RN 的 tus 客户端。

#### 3.2.B 数据落地与环境策略（回答“存哪儿”）

* **二进制文件（STL/OBJ/3MF）**：放在**对象存储**（S3/MinIO/OSS/GCS/Azure Blob）。
* **业务元数据**（名称、版本、sha256、大小、可见性、备注、关联订单等）：放在**关系型数据库**（PostgreSQL/MySQL）。
* **App 端**：仅作**临时缓存**（RN 沙盒目录），上传完成即清理；不做长期持久化。
* **CDN**：可选，绑定只读域名，用于下载预览图与小文件（模型原文件默认不开放直链）。

**环境建议**：

* **开发**：MinIO（自建，单桶 `3d-models-dev`），无 CDN，开启版本化便于回滚，删除策略宽松。
* **测试/预发**：与后端同区域对象存储，开启**版本化**与**生命周期规则**（30 天转低频、180 天归档），每周全量备份。
* **生产**：

  * 境内优先 **阿里云 OSS**（或七牛/腾讯云 COS），全球优先 **AWS S3**；都可用 CDN（CloudFront/OSS CDN）。
  * **加密**：SSE-KMS（云厂商托管密钥）+ 传输层 TLS；
  * **容灾**：跨可用区存储，必要时异地复制（CRR）。

**限制与规范**：

* 单文件上限（MVP 建议）：≤ 500MB；≥ 5MB 走**分片上传**；
* 预签名 URL **有效期**：10 分钟；权限仅 `PutObject`；
* 失败重试与**断点续传**必备（保存已传分片 ETag 清单）；
* 命名规范：`models/{tenantId}/{assetId}/{version}/{originalFileName}`。

#### 3.2.C 安全与合规要点

* 服务端校验扩展名/MIME/大小，计算并保存 **sha256**；必要时走**后端代理上传**以做病毒扫描/DLP。
* 桶策略最小化：默认**私有**；通过**短时预签名 URL**读写；后台定期清理失效 `uploadId`。
* 删除策略：模型版本**软删除** → 30/90 天后**硬删除**（可配置）；审计日志保留 1 年起。
* 日志：记录上传者、IP、设备标识、对象键、ETag/sha256、耗时与失败原因。

#### 3.2.D 续传与网络鲁棒性

* 分片 5–15MB，失败指数退避（200ms → 3s 上限）；
* 切网（Wi‑Fi/蜂窝）可恢复；App 侧保留上传进度，重启后可继续；
* 完成合并前可**取消上传**并回收分片；合并后返回 `file_key` 以写入 `ModelVersion`。

### 3.3 耗材管理（MVP 必备）

* **物料主数据**：材质（PLA/PETG/ABS/TPU/Resin…）、品牌、直径、颜色、密度、计量单位、单价、备注。

* **库存批次**：序列号/批号、仓位、当前数量（g）、状态（在库/报废/冻结）、备注、图片。

* **入出库/盘点**：录入数量变化、原因（采购/领用/盘点调整/报废）、时间与操作者；可填写“关联订单号”。

* **导入导出**：CSV 导入物料/批次；导出库存快照。

* **物料主数据**：材质（PLA/PETG/ABS/TPU/Resin…）、品牌、直径、颜色、密度、计量单位、单价、备注。

* **库存批次**：序列号/批号、仓位、当前数量（g）、状态（在库/报废/冻结）、备注、图片。

* **入出库/盘点**：录入数量变化、原因（采购/领用/盘点调整/报废）、时间与操作者；可填写“关联订单号”。

* **导入导出**：CSV 导入物料/批次；导出库存快照。

## 4. 非功能性（本期约束）

* **性能**：核心列表首屏 < 1.5s；单接口 95 分位 < 300ms（不含文件上传）。
* **可靠性**：失败重试（上传除外）；关键操作可撤销（30s 内本地撤销）。
* **安全**：邮箱+密码登录；JWT；密码强度校验；服务器端审计日志。
* **数据**：文件存储于对象存储（S3/MinIO）；数据库采用事务保证一致性。

## 5. 数据模型（简化）

* **User**(id, name, email, role, active, created_at)
* **Customer**(id, name, email, phone, default_address_id)
* **Address**(id, customer_id, line1..country, zip)
* **ModelAsset**(id, owner_id, name, current_version, visibility, tags[], created_at)
* **ModelVersion**(id, asset_id, version, file_key, file_size, sha256, notes, created_at)
* **Order**(id, customer_id, status, total, currency, due_date, notes, created_at)
* **OrderItem**(id, order_id, model_asset_id?, model_version_id?, external_file_url?, material_type, color, layer_height_mm, qty, unit_price, subtotal)
* **Material**(id, type, brand, diameter_mm, color, density, unit_price, unit, notes)
* **StockLot**(id, material_id, lot_no, serial_no, location, qty_g, state, notes, photo_key)
* **InventoryTxn**(id, lot_id, type[in/out/adjust/scrap], qty_g, reason, related_order_id?, created_at, actor_id)
* **AuditLog**(id, actor_id, entity, entity_id, action, diff, ts)

> 关系：Order-OrderItem(1..n)，ModelAsset-ModelVersion(1..n)，Material-StockLot(1..n)，InventoryTxn-StockLot(n..1)。

## 6. API（示例，REST）

* 认证：`POST /auth/login`
* 订单：`GET /orders`，`POST /orders`，`PATCH /orders/:id/status`，`POST /orders/:id/items`，`GET /orders/export.csv`
* 模型：`GET /models`，`POST /models`，`POST /models/:id/versions`
* 耗材：`GET /materials`，`POST /materials`，`GET /stock/lots`，`POST /stock/lots`，`POST /inventory/txns`
* 上传：`POST /upload`（返回 file_key），写入到 ModelVersion/附件字段

**返回规范**：列表统一 `items[], page, pageSize, total`；写操作返回新资源与 `audit_id`。

## 7. 关键界面（本期）

1. **首页**：概览卡片（待审核订单数、执行中订单、库存低量批次数）。
2. **订单列表/详情**：筛选、批量导入、状态变更、附件；订单行以卡片呈现。
3. **模型库**：网格/列表切换、版本时间线、上传按钮。
4. **耗材库存**：物料与批次列表、入出库记账面板、CSV 导出。

## 8. 开发与测试

* **模式**：前后端并行；OpenAPI 先行与 Mock；E2E 覆盖“创建订单→添加行→状态流转→导出”。
* **质量目标**：关键模块单测覆盖 ≥70%；接口集成测试（状态流转与库存记账）。
* **发布**：内测(Beta) → 小范围灰度 → 全量；崩溃/性能监控（Sentry/埋点）。

## 9. 验收标准（MVP）

* 能完成：订单/模型/耗材三模块的 **新增/编辑/删除/查询** 与 **导入/导出**。
* 订单状态机可按定义流转，并写入审计日志；
* 库存记账台账可回溯到批次与操作者；
* 首屏首开时间 ≤1.5s；
* 100 条订单+100 个模型+100 个批次的数据规模下交互流畅。

## 10. 未来路线（仅列提纲，不在本期）

* 三维预览与轻量可打印性检查；
* 自动报价与成本计算；
* 打印排程、与 OctoPrint/Klipper/设备桥接；
* Shopify/Etsy 等店铺集成；
* 通知中心与消息推送；
* 报表与仪表盘；
* 权限对象级/字段级细化；
* 采购与供应链、预警与预测。
