# 修复部署错误

## 问题

部署脚本执行到 `npm install` 时报错：
```
npm error path /opt/3d-manage-backend/package.json
npm error errno -2
npm error enoent Could not read package.json
```

## 原因

打包文件包含 `backend/` 目录前缀，解压后文件在 `backend/` 子目录中，但部署脚本期望文件直接在根目录。

## 修复

### 方法1：重新上传修复后的文件（推荐）

1. **重新打包**（已在本地完成）
   ```bash
   cd /home/huangjianpei/3D-Manage
   tar -czf backend-deploy.tar.gz --exclude='node_modules' --exclude='.git' -C backend .
   ```

2. **上传修复后的文件**
   ```bash
   scp backend-deploy.tar.gz root@101.37.28.116:/tmp/
   scp server-setup.sh root@101.37.28.116:/tmp/
   ```

3. **在服务器上重新执行**
   ```bash
   ssh root@101.37.28.116
   cd /opt/3d-manage-backend
   
   # 清理旧文件（如果存在）
   rm -rf * .[^.]*
   
   # 重新解压
   tar -xzf /tmp/backend-deploy.tar.gz
   
   # 检查package.json是否存在
   ls -la package.json
   
   # 如果存在，继续安装依赖
   npm install --production
   ```

### 方法2：手动修复服务器上的文件

如果不想重新上传，可以在服务器上手动修复：

```bash
ssh root@101.37.28.116
cd /opt/3d-manage-backend

# 检查目录结构
ls -la

# 如果看到backend/子目录，移动文件
if [ -d "backend" ]; then
    mv backend/* .
    mv backend/.* . 2>/dev/null || true
    rmdir backend
fi

# 验证package.json存在
ls -la package.json

# 继续安装依赖
npm install --production

# 启动服务
pm2 start server.js --name "3d-manage-api"
pm2 save
```

### 方法3：使用修复后的脚本

已更新 `server-setup.sh`，脚本会自动检测并修复目录结构。

重新上传脚本并执行：
```bash
scp server-setup.sh root@101.37.28.116:/tmp/
ssh root@101.37.28.116
/tmp/server-setup.sh
```

## 验证

修复后，验证部署：

```bash
# 在服务器上
cd /opt/3d-manage-backend
ls -la package.json  # 应该存在
npm install --production  # 应该成功

# 测试API
curl http://localhost:3001/health
```

应该返回：`{"status":"OK","timestamp":"..."}`


