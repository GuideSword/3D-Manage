# 手动修复文件路由

## 问题
服务器上的 `routes/files.js` 使用了不兼容 Express 5.x 的路由语法 `router.get('/*', ...)`，导致服务崩溃。

## 解决方案

### 方法1：手动修改服务器上的文件

1. **SSH 登录到服务器**：
```bash
ssh root@101.37.28.116
# 输入密码：huangjianpei123@
```

2. **编辑文件**：
```bash
nano /opt/3d-manage-backend/routes/files.js
```

3. **找到第7行**，将：
```javascript
router.get('/*', async (req, res) => {
```
改为：
```javascript
router.get(/.*/, async (req, res) => {
```

4. **找到第11行**，将：
```javascript
const filePath = req.params[0] || req.path.replace(/^\/api\/files\//, '');
```
改为：
```javascript
const filePath = req.path.replace(/^\/api\/files\//, '').replace(/^\//, '') || req.originalUrl.replace(/^\/api\/files\//, '').replace(/^\//, '');
```

5. **保存文件**（Ctrl+X, 然后 Y, 然后 Enter）

6. **重启服务**：
```bash
cd /opt/3d-manage-backend
pm2 restart 3d-manage-api
```

7. **检查状态**：
```bash
pm2 status
pm2 logs 3d-manage-api --lines 10
curl http://localhost:3001/health
```

### 方法2：使用 scp 上传修复后的文件

1. **从本地上传文件**：
```bash
cd /home/huangjianpei/3D-Manage
scp backend/routes/files.js root@101.37.28.116:/opt/3d-manage-backend/routes/files.js
# 输入密码：huangjianpei123@
```

2. **SSH 登录并重启**：
```bash
ssh root@101.37.28.116
cd /opt/3d-manage-backend
pm2 restart 3d-manage-api
pm2 status
```

## 验证修复

从本地测试：
```bash
curl http://101.37.28.116:3001/health
curl http://101.37.28.116:3001/api/materials
```

如果返回正常数据，说明修复成功！

