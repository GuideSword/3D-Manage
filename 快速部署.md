# 快速部署到服务器 (101.37.28.116)

## 方法1：自动部署（推荐）

已创建了部署脚本，按照以下步骤执行：

### 步骤1：打包代码

```bash
cd /home/huangjianpei/3D-Manage
tar -czf backend-deploy.tar.gz --exclude='node_modules' --exclude='.git' backend/
```

### 步骤2：执行部署脚本

```bash
./deploy-steps.sh
```

脚本会：
- 上传代码到服务器
- 安装 Node.js 和 PM2（如果未安装）
- 安装依赖
- 配置环境变量
- 启动服务
- 配置防火墙

**注意**：执行脚本时会提示输入密码：`huangjianpei123@`

---

## 方法2：手动部署（如果自动脚本失败）

### 步骤1：连接到服务器

```bash
ssh root@101.37.28.116
# 密码: huangjianpei123@
```

### 步骤2：在服务器上执行以下命令

```bash
# 1. 更新系统并安装Node.js
apt-get update && apt-get upgrade -y
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# 2. 安装PM2
npm install -g pm2

# 3. 创建部署目录
mkdir -p /opt/3d-manage-backend
cd /opt/3d-manage-backend

# 4. 上传代码（在本地电脑执行）
# 从本地电脑执行:
# scp backend-deploy.tar.gz root@101.37.28.116:/opt/3d-manage-backend/

# 5. 解压代码（在服务器上）
tar -xzf backend-deploy.tar.gz
rm backend-deploy.tar.gz

# 6. 创建.env文件
cat > .env << EOF
PORT=3001
FRONTEND_URL=*
JWT_SECRET=$(openssl rand -base64 32)
UPLOAD_DIR=./uploads
API_BASE_URL=http://101.37.28.116:3001
EOF

# 7. 创建上传目录
mkdir -p uploads/models uploads/orders/attachments uploads/stock uploads/previews
chmod -R 755 uploads

# 8. 安装依赖
npm install --production

# 9. 启动服务
pm2 start server.js --name "3d-manage-api"
pm2 save
pm2 startup

# 10. 配置防火墙
ufw allow 3001/tcp
ufw reload

# 11. 验证部署
pm2 status
curl http://localhost:3001/health
```

---

## 方法3：使用SSH密钥（免密码，推荐生产环境）

### 步骤1：生成SSH密钥（如果还没有）

```bash
ssh-keygen -t rsa -b 4096
```

### 步骤2：复制公钥到服务器

```bash
ssh-copy-id root@101.37.28.116
# 输入密码: huangjianpei123@
```

### 步骤3：测试免密码登录

```bash
ssh root@101.37.28.116
# 应该不需要密码即可登录
```

### 步骤4：执行部署脚本

```bash
./deploy-steps.sh
# 现在不需要输入密码了
```

---

## 部署完成后

### 验证部署

1. **测试API**：
   ```bash
   curl http://101.37.28.116:3001/health
   # 应该返回: {"status":"OK","timestamp":"..."}
   ```

2. **查看服务状态**：
   ```bash
   ssh root@101.37.28.116 'pm2 status'
   ```

3. **查看日志**：
   ```bash
   ssh root@101.37.28.116 'pm2 logs 3d-manage-api'
   ```

### 前端配置

前端已自动更新为服务器地址：
- API地址: `http://101.37.28.116:3001/api`

### 常用命令

```bash
# 查看服务状态
ssh root@101.37.28.116 'pm2 status'

# 重启服务
ssh root@101.37.28.116 'pm2 restart 3d-manage-api'

# 查看日志
ssh root@101.37.28.116 'pm2 logs 3d-manage-api'

# 停止服务
ssh root@101.37.28.116 'pm2 stop 3d-manage-api'

# 查看实时日志
ssh root@101.28.116 'pm2 logs 3d-manage-api --lines 100'
```

---

## 故障排查

### 如果服务无法启动

```bash
ssh root@101.37.28.116
cd /opt/3d-manage-backend
pm2 logs 3d-manage-api
# 查看错误信息
```

### 如果端口被占用

```bash
ssh root@101.37.28.116
netstat -tulpn | grep 3001
# 查看哪个进程占用了端口
```

### 如果防火墙阻止

```bash
ssh root@101.37.28.116
ufw status
# 确保3001端口已开放
```

---

部署完成后，你的后端服务将在 `http://101.37.28.116:3001` 运行！


