# 服务器修复步骤（修复路由错误）

## 问题
Express 5.x 不支持 `/:filePath(*)` 这种路由语法，需要改为 `/*`

## 修复方法（在服务器上执行）

### 方法1：使用 nano 编辑（推荐）

```bash
# 1. 连接到服务器
ssh root@101.37.28.116

# 2. 进入目录
cd /opt/3d-manage-backend

# 3. 编辑文件
nano routes/files.js
```

在nano编辑器中：

**找到第7行**，将：
```javascript
router.get('/:filePath(*)', async (req, res) => {
```

**改为**：
```javascript
router.get('/*', async (req, res) => {
```

**找到第9行**，将：
```javascript
const filePath = req.params.filePath;
```

**改为**：
```javascript
const filePath = req.params[0] || req.path.replace(/^\/api\/files\//, '');
```

**保存**：
- 按 `Ctrl+O`（保存）
- 按 `Enter`（确认文件名）
- 按 `Ctrl+X`（退出）

**重启服务**：
```bash
pm2 restart 3d-manage-api
pm2 status
```

应该显示 `status: online` 而不是 `errored`

### 方法2：使用 sed 自动替换（更快）

```bash
# 连接到服务器
ssh root@101.37.28.116

# 进入目录
cd /opt/3d-manage-backend

# 备份文件
cp routes/files.js routes/files.js.bak

# 替换路由定义
sed -i "s|router.get('/:filePath(\*)|router.get('/*|g" routes/files.js

# 替换参数获取方式
sed -i "9s|const filePath = req.params.filePath;|const filePath = req.params[0] || req.path.replace(/^\\\\/api\\\\/files\\\\//, '');|g" routes/files.js

# 验证修改
grep "router.get" routes/files.js
grep -A 1 "const filePath" routes/files.js | head -2

# 重启服务
pm2 restart 3d-manage-api

# 查看状态
pm2 status
pm2 logs 3d-manage-api --lines 20
```

### 方法3：上传修复后的文件

**在本地电脑执行**：

```bash
cd /home/huangjianpei/3D-Manage

# 上传修复后的文件
scp backend/routes/files.js root@101.37.28.116:/opt/3d-manage-backend/routes/
# 密码: huangjianpei123@
```

**在服务器上执行**：

```bash
ssh root@101.37.28.116
cd /opt/3d-manage-backend

# 重启服务
pm2 restart 3d-manage-api

# 查看状态
pm2 status
```

## 验证修复

```bash
# 在服务器上测试
curl http://localhost:3001/health

# 应该返回：
# {"status":"OK","timestamp":"..."}

# 从本地电脑测试
curl http://101.37.28.116:3001/health
```

## 如果还是报错

查看日志：
```bash
pm2 logs 3d-manage-api --err --lines 30
```

把错误信息发给我。


